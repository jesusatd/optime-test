{% form_theme form 'bootstrap_5_layout.html.twig' %}

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="form-group">
            <a href="{{ path('category.index') }}" class="btn btn-light" style="margin-right:.6em">Cancel</a>
            <button  type="submit" class="btn btn-success">Save</button>
        </div>
    {{ form_end(form) }}